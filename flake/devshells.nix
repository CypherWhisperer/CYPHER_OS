# flake/devshells.nix
#
# devShells.<system> — `nix develop` environments.
# Currently empty; nixamp and other devenvs may live here or stay
# independent depending on exposure design.

{
  inputs,
  system,
  pkgs,
}:
{ }
